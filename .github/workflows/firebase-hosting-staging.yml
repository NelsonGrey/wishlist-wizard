name: "Deploy Web App to Staging (Hosting)"

# Branch promotion path: develop → staging → main. Push to staging deploys
# the compiled dist/ bundle to Firebase Hosting on wishlist-wizard-staging.
# Business logic lives in Firebase Functions (private, server-side). This
# workflow publishes only the compiled dist/ bundle; no source is exposed.

permissions:
  contents: read

on:
  push:
    branches:
      - staging

concurrency:
  group: firebase-hosting-staging-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy-hosting:
    name: Build and Deploy to Firebase Hosting (Staging)
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: "20"
          cache: npm

      - name: Install Firebase CLI
        run: |
          npm install -g firebase-tools
          firebase --version

      - name: Install Dependencies
        run: |
          npm ci --legacy-peer-deps 2>/dev/null || npm ci --force
          npm run build --workspace=@wishlist-wizard/shared
          npm run build --workspace=@shared/firebase-utils

      - name: Build Web App
        env:
          VITE_FIREBASE_API_KEY: ${{ secrets.VITE_FIREBASE_API_KEY_STAGING }}
          VITE_FIREBASE_AUTH_DOMAIN: ${{ secrets.VITE_FIREBASE_AUTH_DOMAIN_STAGING }}
          VITE_FIREBASE_PROJECT_ID: ${{ secrets.VITE_FIREBASE_PROJECT_ID_STAGING }}
          VITE_FIREBASE_STORAGE_BUCKET: ${{ secrets.VITE_FIREBASE_STORAGE_BUCKET_STAGING }}
          VITE_FIREBASE_MESSAGING_SENDER_ID: ${{ secrets.VITE_FIREBASE_MESSAGING_SENDER_ID_STAGING }}
          VITE_FIREBASE_APP_ID: ${{ secrets.VITE_FIREBASE_APP_ID_STAGING }}
          VITE_FIREBASE_MEASUREMENT_ID: ${{ secrets.VITE_FIREBASE_MEASUREMENT_ID_STAGING }}
          # Must be the _STAGING-suffixed var name specifically (not the
          # plain name used above): App Check's site key isn't part of
          # Firebase Hosting's runtime /__/firebase/init.json auto-config, so
          # there's no fallback if this isn't baked in correctly at build
          # time, unlike the plain-named fields above.
          VITE_FIREBASE_APPCHECK_SITE_KEY_STAGING: ${{ secrets.VITE_FIREBASE_APPCHECK_SITE_KEY_STAGING }}
        run: npm run build --workspace=@wishlist-wizard/web

      - name: Use Staging Firebase Config
        run: cp firebase.staging.json firebase.json

      - name: Deploy Firebase Hosting
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
        run: |
          if [ -z "$FIREBASE_TOKEN" ]; then
            echo "FIREBASE_TOKEN secret is not set"
            exit 1
          fi
          firebase deploy \
            --project="wishlist-wizard-staging" \
            --token="$FIREBASE_TOKEN" \
            --only=hosting \
            --message="Staging hosting deploy from staging — ${{ github.sha }}" \
            --non-interactive
