specVersion: "v1"
serviceId: "wishlist-wizard"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "wishlist-wizard-fdc"
        # schemaValidation: "STRICT"     # STRICT mode makes Postgres schema match Data Connect exactly.
        # schemaValidation: "COMPATIBLE" # COMPATIBLE mode makes Postgres schema compatible with Data Connect.
connectorDirs: []
# Note: ./example directory disabled - uses deprecated auth.uid syntax.
# Example queries should be updated to use uuidV4() or app-level auth handling.
